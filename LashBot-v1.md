# LashBot-v1 - навивочная машинка

Ниже представлена концепция, спецификация и базовый код.
1. Концепция
LashBot — это автономная навивочная машинка для монтажа абонентского дроп-кабеля на существующую оптическую магистраль.

* Принцип: Машинка подвешивается на несущий кабель. При движении вперед (ручном или моторном) переднее колесо вращается.
* Автоматизация: Датчик Холла фиксирует оборот колеса $\rightarrow$ Контроллер дает команду сервоприводу провернуть катушку $\rightarrow$ Дроп-кабель равномерно укладывается вокруг магистрали.
* Особенность: Допускается проскальзывание; расчет на самораспределение витков.

------------------------------
2. Перечень деталей (BOM)

| Деталь | Тип/Модель | Зачем нужна |
|---|---|---|
| Контроллер | Arduino Nano / ESP32 | «Мозги» системы |
| Датчик | Датчик Холла (модуль KY-003) | Считает обороты ведущего колеса |
| Магнит | Неодимовый (диск 5-10 мм) | Устанавливается на ведущее колесо |
| Привод | Серво 360° (напр. MG996R 360) | Вращает катушку с дроп-кабелем |
| Питание | Li-ion 18650 (2 шт.) + Holder | 7.4V для питания логики и серво |
| Понижайка | DC-DC LM2596 (опционально) | Если серво нужно строго 5-6V |
| Корпус | 3D-печать / Алюминиевый профиль | Несущая рама и захваты для кабеля |
| Колеса | Ролики с резиновым ободом | Для сцепления с кабелем |

1. Механическая концепция (V3)
Основа: Отрезок ПНД-трубы (длиной 30–50 см), разрезанный вдоль. Он надевается на магистральный кабель.
Передняя бабка (Тяговый узел): Неподвижный корпус, который скользит по кабелю. В нем живут: АКБ, Arduino, датчик Холла и сервопривод.
Передача: Сервопривод через ролик или шестерню вращает саму ПНД-трубу вокруг кабеля.
Катушка: Закреплена прямо на ПНД-трубе. Когда труба вращается — кабель наматывается вокруг магистрали.
Привод: Вы тянете за веревочку, привязанную к передней бабке. Колесо в бабке крутится от трения о кабель, датчик считает метры, а серво крутит трубу с катушкой.
2. Перечень деталей (BOM)
Несущая часть: ПНД-труба (диаметр 32 или 40 мм) с продольным пропилом.
Электроника:
Arduino Nano (внутри передней бабки).
Датчик Холла + магнит (на ведущем колесе бабки).
Сервопривод 360° (MG996R) с резиновым роликом, прижатым к ПНД-трубе.
АКБ 18650 (2 шт).
Механика:
Подшипники или скользящие втулки, в которых вращается ПНД-труба относительно бабки.
Катушка (щечки), закрепленные на трубе.
3. Код Arduino (Адаптация под вращение трубы)
```
cpp
#include <Servo.h>

const int HALL_PIN = 2;   // Датчик на колесе бабки
const int SERVO_PIN = 9;  // Привод, вращающий трубу
const int SPEED_VAL = 110; // Скорость вращения трубы
const int ROTATE_TIME = 800; // Время вращения трубы на один "шаг" движения

Servo pipeServo;
volatile bool trigger = false;

void setup() {
  pinMode(HALL_PIN, INPUT_PULLUP);
  pipeServo.attach(SERVO_PIN);
  pipeServo.write(90); // Стоп
  attachInterrupt(digitalPinToInterrupt(HALL_PIN), onStep, FALLING);
}

void loop() {
  if (trigger) {
    // Вращаем ПНД-трубу с катушкой
    pipeServo.write(SPEED_VAL);
    delay(ROTATE_TIME); 
    pipeServo.write(90); // Остановка
    
    trigger = false;
  }
}

void onStep() {
  trigger = true;
}

```
